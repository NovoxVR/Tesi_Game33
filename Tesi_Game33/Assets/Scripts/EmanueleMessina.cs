using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EmanueleMessina : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("Luca Tiengo");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
